{
  "name": "portfolio-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "./build.sh && tailwindcss -i ./src/input.css -o ./src/output.css --minify",
    "build:css": "tailwindcss -i ./src/input.css -o ./src/output.css --minify",
    "serve": "python3 -m http.server 8080 -d ./src"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
